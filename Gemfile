source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }
gem 'rest-client'

group :test do
  gem 'webmock'

end

# Specify your gem's dependencies in metropol_api.gemspec
gemspec
